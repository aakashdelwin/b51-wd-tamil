let promise=new promise((resolve,reject) =>{
    let result=10;
    if(result>10){
        resolve(result);
    }else{
        reject(result is not greater than 10);
    }
})

promise
     .then()
        console
